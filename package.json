{
  "name": "zenithdao",
  "version": "1.0.0",
  "private": true,
  "description": "ZenithDAO — on-chain DAO governance protocol. Govern without limits.",
  "license": "MIT",
  "scripts": {
    "backend:dev": "pnpm --filter @zenithdao/backend dev",
    "backend:build": "pnpm --filter @zenithdao/backend build",
    "frontend:dev": "pnpm --filter @zenithdao/frontend dev",
    "frontend:build": "pnpm --filter @zenithdao/frontend build",
    "contracts:build": "pnpm --filter @zenithdao/contracts build",
    "contracts:test": "pnpm --filter @zenithdao/contracts test"
  },
  "engines": {
    "node": ">=20"
  }
}
